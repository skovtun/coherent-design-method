# Story 3.13: E2E Smoke Test (All Tracks)

## Status

Draft

---

## Story

**As a** maintainer,  
**I want** a single E2E smoke test that covers Export, Quality, and Figma import so that we confirm all three tracks work before launch.

---

## Acceptance Criteria

- [ ] Export E2E: init → chat (3 pages) → export → build exported project
- [ ] Quality E2E: generate all 12 template types → validate → score
- [ ] Figma E2E: import test file → preview → modify → export
- [ ] `pnpm test:quality` (or equivalent) updated to include these

---

## Details (from Epic 3)

**Full test:**

1. **Export:** init → chat (e.g. 3 pages) → export → build exported project (success)
2. **Quality:** generate all 12 template types → validate → score (e.g. ≥8/10 or no critical failures)
3. **Figma:** import test file (fixture or small public file) → preview (or build) → modify via chat → export

All three tracks must pass. Story 3.13 waits for 3.1–3.2 (A), 3.3–3.7 (B), 3.8–3.12 (C).

---

## Tasks / Subtasks

- [ ] Add or extend E2E script for Track A: init, chat, export, build
- [ ] Add or extend for Track B: 12 templates, validate, optional score assertion
- [ ] Add or extend for Track C: import figma (with test file/token or mocked data), preview/build, chat, export
- [ ] Integrate into `pnpm test:quality` or CI
- [ ] Document test file and token requirements for Figma (or use mock)
- [ ] Verify AC

---

## Dev Notes

- Depends on completion of 3.1–3.12. Figma test may require a stable test file and token in env (or mock API).
- Quality smoke test update is also in 3.7; 3.13 is the full E2E that ties all tracks together.

---

## Change Log

| Date       | Version | Description | Author |
|------------|---------|-------------|--------|
| 2026-01-26 | 0.1     | Story created (Epic 3, all tracks). | — |

---

## Dev Agent Record

### Agent Model Used

—

### Completion Notes List

—

### File List

—

---

## QA Results

—
